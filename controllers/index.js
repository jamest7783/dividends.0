const equity=require('./equityController')
const account=require('./accountController')
const thread=require('./threadController')
const reply=require('./replyController')

module.exports={
    equity,
    account,
    thread,
    reply
}
